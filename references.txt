# Reference clones under ~/Projects/quarry that the maintenance ledger's source
# checks read. One per line: <directory> <git URL>. The sibling repositories'
# make refs updates existing family-listed clones and reports/keeps unlisted
# clones. Missing entries are created only from the invoking sibling's own
# manifest, so references needed here are declared here and bootstrapped
# separately with explicit approval.
opencode https://github.com/anomalyco/opencode.git
